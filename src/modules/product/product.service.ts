import { ProductServiceRepository } from "./product.repository"
import { CreateProductServiceTypes } from "../../common/schemas/product.schemas"

export const ProductServiceService = {
    // async create(data: CreateProductServiceTypes, storeId: string) {
    //     const product = await ProductServiceRepository.create(data, storeId)
    // }
}